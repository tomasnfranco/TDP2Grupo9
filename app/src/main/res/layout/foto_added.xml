<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/foto_added" android:layout_width="@dimen/foto_publicacion_size"
    android:layout_height="@dimen/foto_publicacion_size" android:layout_marginTop="5dp"
    android:layout_marginRight="5dp" android:layout_marginBottom="5dp" android:padding="0dp">

    <ImageView
        android:id="@+id/foto_image_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"/>

    <View
        android:layout_width="@dimen/delete_foto_publicacion_size"
        android:layout_height="@dimen/delete_foto_publicacion_size"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:background="@drawable/delete_background"/>

    <ImageView
        android:id="@+id/delete_foto_image_view"
        android:layout_width="@dimen/delete_foto_publicacion_size"
        android:layout_height="@dimen/delete_foto_publicacion_size"
        android:src="@drawable/ic_delete"
        android:adjustViewBounds="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:scaleType="fitCenter"/>

</RelativeLayout>